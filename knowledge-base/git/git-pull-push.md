# Git Pull and Push

## Definition

`git pull` fetches and merges remote changes; `git push` uploads local commits.

## Explanation

These commands synchronize local and remote repositories.

## Example

Pull latest changes before pushing your work.

## Command block

```bash
git pull origin main
git push origin main
```

## Real-world use case

Collaborators pull updates and push contributions in team projects.