# Git Branch

## Definition

`git branch` manages branches in a Git repository.

## Explanation

Branches allow parallel development without affecting the main codebase.

## Example

Create a new branch called `feature-x` for new features.

## Command block

```bash
git branch feature-x
git checkout feature-x
```

## Real-world use case

Teams create branches for each feature to isolate changes and enable code reviews.