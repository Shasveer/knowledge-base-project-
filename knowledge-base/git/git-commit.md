# Git Commit

## Definition

`git commit` saves changes to the repository with a message.

## Explanation

Commits create snapshots of the project at a point in time.

## Example

Stage files and commit with a descriptive message.

## Command block

```bash
git add .
git commit -m "Add new feature"
```

## Real-world use case

Developers commit regularly to preserve progress and enable rollbacks.